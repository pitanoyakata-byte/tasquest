# タスクエスト（仮称）— プロジェクト指示書

このファイルはClaude Codeが実装時に毎回参照する前提の指示書。方針や仕様に矛盾・不足を見つけた場合は、仮決めで実装を進めず、必ず開発者（しょうたさん）に確認すること。

## 参照ドキュメント

- `タスクエスト企画書_v19.docx`（またはそれ以降の最新版）：何を作るかの合意文書
- `screens.md`：どう実装するかの画面仕様書
- 本ファイルの内容と上記2つが矛盾する場合は、まず開発者に確認する（本ファイルを黙って優先しない）

## 開発の基本方針（絶対に破らないルール）

- **依存させるが、強制しない**：ダークパターン、恐怖訴求（「今解約すると消えます」等）、偽の演出（当たったように見せて実は外れ、など演出と結果が食い違うもの）は一切実装しない。
- **失敗を作らない**：達成の有無と達成度0〜10を混同しない。達成度は振り返り専用で、報酬計算に一切使わない。
- **ステータス間の公平性**：体力・知力・生活力のどれか1つに偏っても、受注条件・報酬のいずれにも不利益が出ないようにする。
- **課金の誠実性**：無課金ユーザーのデータ（達成度記録・日記・経験値など）は非表示になることはあっても削除はしない。

## UI・多言語

- UIに表示するテキストは、コード中に直接文字列を書かず、必ずテキストID経由で管理する。
- 対応言語：`ja` `en` `ko` `zh-Hans` `de` `fr` `it` `es` の8言語。言語コードごとのJSONファイル（例：`i18n/ja.json`）でテキストIDとの対応表を持つ。
- 新しいUI要素を追加する際は、テキストIDを先に決めてから8言語ぶんのJSONにキーを追加する（1言語だけ追加して後回しにしない）。

## データ・バックアップ運用

- Firestoreのフィールド追加・削除・型変更など、既存データに影響する変更を行う前には、必ず移行方針を明文化し、可能であれば既存データのバックアップ／移行スクリプトを用意してから着手する。
- スキーマに関わる変更をコミットする際は、コミットメッセージに「何のデータに、どう影響するか」を明記する。

## 進捗の記録

- 作業を行うたびに `PROGRESS.md`（このファイルと同じディレクトリ、なければ新規作成）に日時・やったこと・次にやることを追記する。長い会話の圧縮が起きても、この記録を読めば経緯を追えるようにする。
- git commitは1コミット1目的を意識し、小さく分ける。コミットメッセージには「何を・なぜ」を書く。

## 技術方針

- 開発フレームワーク：Flutter（iOS/Android対応）
- データ管理：Firebase／Firestore
- 認証：Firebase Authentication（匿名認証で開始し、必要になった時点でApple／Googleアカウントに紐付ける）
- 課金：RevenueCat経由でApple（StoreKit）・Googleのサブスクリプションを統合管理
- 通知：Firebase Cloud Messaging
- 広告：Google AdMob
- 依存パッケージ・実装パターンは、Flutter／Firebase公式ドキュメントの現行の安定版・推奨パターンを優先する。非推奨（deprecated）のAPIやパッケージ、メンテナンスが止まっているパッケージは避ける。迷った場合は最新の公式ドキュメントを確認してから採用する。

## 未決定の技術選定（実装開始時に決めてよい）

- 状態管理の方式（Riverpod、Bloc等。現行のFlutterコミュニティで広く使われている選択肢から、公式ドキュメントの推奨状況を見て選ぶ）
- ディレクトリ構成・アーキテクチャパターン
- テスト方針（ユニットテスト・ウィジェットテストの範囲）

これらは「作りながら決めてよい」項目として、企画書・画面仕様書側では確定させていない。実装を始める段階で、その時点のFlutterのベストプラクティスに沿って選定すること。
