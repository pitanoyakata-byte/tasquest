# PROGRESS.md — タスクエスト（仮称）開発記録

CLAUDE.mdの規約に基づき、作業のたびにここへ日時・やったこと・次にやることを追記する。

---

## 2026-08-24（企画書v19・screens.md改訂の反映）

### やったこと

- 開発者からCLAUDE.md・screens.md・企画書v19の改訂版を受領。リポジトリ内の参照ドキュメントを更新
  （`タスクエスト企画書_v15.docx`→`_v19.docx`、`screens.md`、`CLAUDE.md`のバージョン表記）。コードの変更はなし。
- 主な変更点（v15→v19）：
  - **称号機能を実装見送りに決定**（費用対効果が薄いと判断）。図鑑（美術品コレクション）のみ残る
  - **図鑑（企画書5-4）の仕様が大幅に具体化**：パブリックドメインの実在美術品を題材にし、架空通貨「ドルドル」建てのコレクション価値、レア度＝価値連動、コレクション総評価額の表示など
  - **装備（5-3）に明確な数値仕様が追加**：`equipmentValue`（絶対値、ステータスごとの最高値を保持）
  - **町の成長（5-7）が仕様変更**：旧版は合計レベル基準の単一の町ステージだったが、新版は体力＝ジム／知力＝研究施設／生活力＝自宅の3棟が、それぞれ対応ステータスのレベルが5の倍数に達するたびに個別にグレードアップできる方式に変更
  - **「表示ステータス値」計算式が新規追加（5-4-2）**：装備の数値×タウン補正×レベル補正×美術品補正の掛け算。ゲームプレイ判定（受注条件・報酬）には使わない、あくまで表示用
- 開発者に確認：表示ステータス値の「レベル補正（1.01^L）」のLは、無課金者のレベルキャップ（Lv50）でクランプされた表示上のレベルを使う。課金すると本来の（キャップなしの）レベルに切り替わる。裏側の経験値・本来のレベルはキャップ中も常に計算し続ける（既存の`StatProgress.displayLevel(isPaidUser)`の考え方をそのまま踏襲すればよいことを確認）。
- ROADMAP.mdを更新：Phase1・2を完了扱いに更新。装備・町の成長・表示ステータス値の計算ロジックは「課金限定機能ではなくコア機能」と整理し、図鑑（美術品）本体より先行して実装できる旨をPhase5に追記。Phase7から称号を削除。

### 次にやること

- 装備ドロップ・町の成長（3棟）・表示ステータス値の実装方針を詰めて着手する（Phase5、開発者と実装順序を最終確認してから）
- 図鑑（美術品）はコンテンツ制作（作品選定・画像ライセンス確認・価値テーブル作成）が開発者側で必要。着手時期は別途相談

---

## 2026-08-24（装備・町の成長・表示ステータス値の実装）

### やったこと

- 開発者確認：装備ドロップの釘落とし演出は別途企画中（本セッションのスコープ外）。確率・数値テーブルも企画側で再調整して後日提出予定。今回は**演出なし・ダミーの確率＆数値で「報酬が手に入る」ことだけ**を再現する形で実装した。
- `UserProfile`に`equipmentValue`（装備の上乗せ数値、ジャンル別・絶対値）と`townLevels`（町のグレードアップ回数、ジャンル別）を追加。Firestore・ローカル両リポジトリのシリアライズに反映（既存ユーザーデータは無いため移行作業は不要。将来的に既存ユーザーがいる状態でフィールドを増やす場合は、この変更が前例になる）
- `DisplayStat`（表示ステータス値の計算：装備×タウン補正×レベル補正×美術品補正）を実装。美術品補正は図鑑未実装のため1.0固定。企画書5-4-2の計算例（体力Lv56・装備1000・タウンLv11・美術品補正1.265→約3193）と一致することをユニットテストで確認
- 装備ドロップをダミー実装：`UserProfileController.completeActiveQuest`内で、クエスト達成時に仮の確率（通常30%／クイック枠15%、いずれも**要調整の仮数値**）でジャンル重み50/25/25に従い装備価値を更新。報酬画面に「{ステータス}の装備価値が更新されました」という結果を表示（釘落とし演出は無し、テキストのみ）
- 町の成長（企画書5-7）を実装：ホーム画面に、対応するステータスの表示レベルが5の倍数に達したステータスぶんだけ「グレードアップ」ボタンが出現。タップで該当ジャンルの`townLevels`が+1され、表示ステータス値のタウン補正に反映される
- マイページのステータスタブに「総合力（表示ステータス値）」を追加表示（レベルバーとは別枠、企画書6章の要求どおり両方表示）
- 開発者確認済み：表示ステータス値の「レベルの補正」に使うレベルは、無課金者は表示上のキャップ済みレベル（`displayLevel`）を使う。町の成長ボタンの出現条件も同じく`displayLevel`で判定するようにした（企画書に明記されていない部分だが、レベルキャップの既存方針と一貫させた。この解釈で問題なければそのまま、違えば要修正）
- `flutter analyze` → 0件、`flutter test` → 全パス（表示ステータス値の計算式を検証するユニットテストを追加）

### 次にやること

- 装備ドロップの確率・数値テーブルが企画側で確定次第、`UserProfileController._rollDummyEquipmentDrop`のダミー実装を差し替える
- 釘落とし演出（ミニゲーム）の仕様が固まり次第、報酬画面に組み込む
- 大連続クエスト・自作クエスト・数値記録テンプレートなど、Phase5の残りに着手する

---

## 2026-08-23（初回セッション）

### やったこと

- 企画書v15・CLAUDE.md・screens.mdを読み込み、要件を確認。
- 開発コンテナにFlutter SDK（stable channel）を導入（`/tmp/flutter_sdk`。ユーザーの手元PCには別途導入が必要、後述）。
- `flutter create` でプロジェクト雛形を作成（Android/iOS/Web対応）。
- 技術選定（CLAUDE.md「未決定の技術選定」に対する回答）：
  - 状態管理：**Riverpod**（`flutter_riverpod`）。コード生成なしのシンプルな`AsyncNotifier`構成。
  - 画面遷移：**go_router**。
  - ローカル永続化（Firebase導入までの仮の保存先）：`shared_preferences`。将来Firestoreに差し替える前提で`UserRepository`インターフェースを挟んである。
  - ディレクトリ構成：機能別（`lib/features/{onboarding,checkin,quest_active,reward,home}`）＋共通基盤（`lib/core`）＋データ層（`lib/data`）。
- i18nテキストID基盤を実装（`lib/core/i18n/`）。`assets/i18n/{ja,en,ko,zh-Hans,de,fr,it,es}.json` を8言語分すべて作成し、テキストIDで文言を引く`tt(ref, id)`ヘルパーを用意。今回追加した約34個のテキストIDはすべて8言語分そろえた。
- コアゲームループ（画面仕様書1〜5章のうち、mode=normal/tutorialの範囲）を実装：
  - `welcome`（オンボーディング）
  - `checkin`（気分5択・チュートリアルガイド文）
  - `quest_active`（ロックタイマー、1秒更新、経過で自動遷移）
  - `reward`（やったボタン→気分→達成度0〜10→報酬確定→結果表示。**報酬は演出より先にサーバー側相当のロジックで確定**してから表示する設計にしてあり、企画書5-3-1の「演出と結果の食い違いを作らない」原則を満たす）
  - `home`（ステータス3種の表示、おすすめクエスト一覧＝固定のサンプル3件）
- 起動時の画面分岐（画面仕様書0-2）を`GoRouter`のスプラッシュ画面で実装。`hasSeenWelcome` / `appState` / ロック経過判定で自動的に正しい画面へ振り分ける。
- 経験値カーブ（企画書5-2：必要経験値(L) = 50 × 1.122^(L-1)、1回の報酬 = 必要経験値/2、クイック枠は/4）と無課金レベルキャップ（Lv50、経験値自体は常に加算し表示のみクランプ）を実装。
- `flutter analyze` → 0件、`flutter test`（Welcome→Checkin遷移のスモークテスト）→ 全パス。

### 意図的にPhase1のスコープ外にしたもの（あとで実装する）

- **Firebase／Firestore／Firebase Authentication**：Firebaseプロジェクト未作成のため。現在はSharedPreferencesのローカル保存のみ。`UserRepository`インターフェースを挟んであるので、Firestore版の実装を追加してDIを差し替えるだけで移行できる想定。
- **RevenueCat課金・広告（AdMob）**：「広告を見てスキップ」「広告を見て報酬2倍」ボタンは、実際にSDKが繋がっていない状態で偽の演出を出すと企画書の「誠実性」原則に反するため、Phase1では表示していない。SDK導入時に追加する。
- **報酬演出（釘落とし）**：物理演算演出は未実装。現在は結果を即座にテキスト表示するのみ（結果を先に確定してから見せる、という誠実性の原則自体は満たした簡易版）。
- **大連続クエスト・数値記録テンプレート・図鑑・称号・マイページ・設定画面・consent（CMP/ATT）・paywall**：画面仕様書6章以降は未着手。
- **クエストのクールタイム（1時間）判定**：Phase1のサンプルクエストには未実装。

### 次にやること（次回セッションの入り口）

1. Firestoreセキュリティルール（`firestore.rules`）をデプロイする（下記「あなたにお願いしたいこと」参照）。デプロイするまでは、アプリからFirestoreへの読み書きが全て拒否された状態のまま。
2. デプロイ後、ノートPC側で`flutter run -d chrome`して、実際にFirestoreにデータが書き込まれるか確認（Firebaseコンソールの Firestore Database → データ タブで`users`コレクションにドキュメントができているか見る）。
3. マイページ（ステータスタブ）・設定タブの雛形を追加し、下部タブバーで3画面を行き来できるようにする（ROADMAP.md Phase2）。
4. クエストのクールタイム判定・広告ゲート（4件目以降）など、ホーム画面の未実装ロジックを足していく。

### あなたにお願いしたいこと（Firestoreセキュリティルールのデプロイ）

ノートPCのターミナルで、プロジェクトフォルダに移動してから以下を実行してください。

```
cd C:\src\tasquest
firebase use tasquest-abbad
firebase deploy --only firestore:rules
```

これでFirestoreのセキュリティルール（`firestore.rules` — 「本人だけが自分のデータを読み書きできる」というルール）が反映されます。完了したら教えてください。

---

## 2026-08-23（Firebase連携）

### やったこと

- ユーザー側でFirebaseプロジェクト（プロジェクトID：`tasquest-abbad`）を作成
  - Authentication：匿名認証を有効化
  - Firestore Database：作成（ロケーション：東京、本番環境モード）
- `flutterfire configure`でFlutterアプリとFirebaseプロジェクトを接続（Android/iOS/Webの3プラットフォーム分）。`lib/firebase_options.dart`・`android/app/google-services.json`・`firebase.json`をリポジトリに追加
- アプリ側の実装：
  - `firebase_core`・`firebase_auth`・`cloud_firestore`を導入
  - `main.dart`でFirebase初期化＋匿名サインイン（`ensureAnonymousSignIn`）を実行
  - `UserRepository`インターフェースをuid引数を取る形に変更し、`FirestoreUserRepository`を実装（`users/{uid}`とサブコレクション`users/{uid}/questInstances/{instanceId}`に保存）
  - `authUidProvider`を新設し、本番はFirebase匿名認証、テストは固定uidでoverrideする構成にした（テストがFirebase接続なしで動くように分離）
  - `firestore.rules`を作成：本人（`request.auth.uid`が一致するuid）のみ自分のドキュメントを読み書き可能、`quests`マスタは全員読み取りのみ
- `flutter analyze` → 0件、`flutter test` → 全パス（Firestore未接続でもテストは動く構成）

### つまずいたポイント（記録として）

- ノートPC（Windows）でのセットアップで、環境変数PATHの編集が保存されずに何度か消える現象があった → 最終的には手順通りで解決（原因は保存前に閉じてしまっていたことの可能性）
- `git commit`時にVimエディタが開き、日本語IMEがオンだったため`:wq`等のコマンドが全角文字になり操作不能に → IMEを半角に切り替えることで解決。今後は`git config --global core.editor notepad`でメモ帳を使う設定に変更済み

### 次にやること

上記「次にやること（次回セッションの入り口）」を参照。

---

## 2026-08-23（Phase2：マイページ・設定・下部タブ）

### やったこと

- ホーム／マイページ／設定を行き来する下部タブ（`HomeShell`）を実装
- マイページ：ステータスタブ（3ステータスのレベル・経験値バー＋合計レベル）、振り返りタブ（完了済みクエストの履歴一覧）
  - `UserRepository`に`fetchQuestHistory`を追加。無課金者は直近1週間分のみに絞る（企画書15章）。Firestoreの複合インデックス（`firestore.indexes.json`）も追加
- 設定画面：言語切替（8言語、実際に切り替わる）のみ本実装。音量・通知・サブスクリプション管理・アカウント管理・利用規約等は、対応するSDK/画面がまだ無いため「準備中」の表示にとどめた（動かない機能を動くように見せない、という方針を優先）
- 新規テキストID20個を8言語ぶん追加
- `flutter analyze` → 0件、`flutter test` → 全パス（下部タブの遷移テストを追加。SharedPreferencesのプラグインキャッシュがテストファイル間で残る問題があったため、このテストは別ファイル`test/home_shell_test.dart`に分離した）

### 次にやること

- ROADMAP.md Phase3（RevenueCat課金・paywall・consent）に進む、または他の相談事項を先に進める
- 設定画面の「準備中」項目は、対応するPhaseで順次実装に置き換える

---
